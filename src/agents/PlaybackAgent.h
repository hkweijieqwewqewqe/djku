#ifndef PLAYBACK_AGENT_H
#define PLAYBACK_AGENT_H

class PlaybackAgent {
public:
    PlaybackAgent() = default;
    // Control playback of audio
    void play();
    void pause();
};

#endif // PLAYBACK_AGENT_H
